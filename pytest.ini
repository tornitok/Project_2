[pytest]
addopts = -ra -q
markers =
    live: marks tests that hit the real Stellar Burgers API (deselect with -m "not live")
    smoke: quick smoke tests
filterwarnings =
    ignore::DeprecationWarning

